code: cmovnzw (%rax), %bx

  maybe read:      { %rax %zf }
  must read:       { %rax %zf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnzw (%rax), %bx: Hindex0(10)
cmovnzw (%rax), %bx: Hindex0(10)
cmovnzw %cx, %bx: Hindex3(9)
Formula:

%rbx   : %rbx[63:16] ∘ (!%zf ? TMP_BV_16_0 : %rbx[15:0])

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
