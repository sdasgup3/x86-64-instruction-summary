code: movq $0x0, (%rax)

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movq $0x0, (%rax): Hindex0(10)
movq $0x0, (%rax): Hindex0(10)
movq %rcx, %rbx: Hindex5(9)
Formula:

Information about memory writes:
  Address %rax was updated to
    0x0₆₄ (8 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv ∨ !sigfpe ∧ !sigbus ∧ false
