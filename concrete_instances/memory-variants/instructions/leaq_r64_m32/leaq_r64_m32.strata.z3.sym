code: leaq (%rcx), %rbx

  maybe read:      { %rcx }
  must read:       { %rcx }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

leaq (%rcx), %rbx: Hindex5(10)
leaq (%rcx), %rbx: Hindex5(10)
Formula:

%rbx   : %rcx

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
