code: rolb $0x1, (%rax)

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { %cf %of }
  must write:      { %cf %of }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

rolb $0x1, (%rax): Hindex0(10)
rolb $0x1, (%rax): Hindex0(10)
rolb $0x1, %bl: Hindex7(9)
Formula:

%cf    : true ∧ (TMP_BV_8_1 rot_left 0x1₈)[0:0] = 0x1₁ ∨ false ∧ %cf
%of    : true ∧ ((TMP_BV_8_1 rot_left 0x1₈)[7:7] = 0x1₁ ⊕ (TMP_BV_8_1 rot_left 0x1₈)[0:0] = 0x1₁) ∨ false ∧ TMP_BOOL_0

Information about memory reads:
  Value TMP_BV_8_0 (1 bytes)
    was read at address %rax.
  Value TMP_BV_8_1 (1 bytes)
    was read at address %rax.

Information about memory writes:
  Address %rax was updated to
    TMP_BV_8_1 rot_left 0x1₈ (1 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv ∨ !sigfpe ∧ !sigbus ∧ false
