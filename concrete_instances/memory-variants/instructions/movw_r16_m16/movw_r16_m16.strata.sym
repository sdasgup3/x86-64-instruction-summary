code: movw (%rax), %bx

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { %bx }
  must write:      { %bx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movw (%rax), %bx: Hindex0(10)
movw (%rax), %bx: Hindex0(10)
movswq %cx, %rbx: Hindex5(9)
Formula:

%rbx   : (concat ((_ extract 63 16) %rbx) TMP_BV_16_0)

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
