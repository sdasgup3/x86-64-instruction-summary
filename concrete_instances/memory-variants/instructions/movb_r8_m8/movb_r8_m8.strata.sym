code: movb (%rax), %bl

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movb (%rax), %bl: Hindex0(10)
movb (%rax), %bl: Hindex0(10)
movb %cl, %bl: Hindex5(9)
Formula:

%rbx   : %rbx[63:8] ∘ TMP_BV_8_0

Information about memory reads:
  Value TMP_BV_8_0 (1 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
