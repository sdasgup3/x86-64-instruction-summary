code: cmovncw (%rax), %bx

  maybe read:      { %rax %cf }
  must read:       { %rax %cf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : %rbx[63:16] ∘ ((%cf ? 0x100₁₆ : 0x0₁₆) = 0x0₁₆ ? TMP_BV_16_0 : %rbx[15:0])

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
