code: rorxl $0x0, (%rax), %ebx

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { bmi2 }

rorxl $0x0, (%rax), %ebx: Hindex2(10)
rorxl $0x0, (%rax), %ebx: Hindex2(10)
Formula:

%rbx   : (concat #x00000000
        (bvor (bvlshr TMP_BV_32_0 #x00000000) (bvshl TMP_BV_32_0 #x00000020)))

Information about memory reads:
  Value TMP_BV_32_0 (4 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (or sigsegv (and (not sigfpe) (not sigbus) false))
