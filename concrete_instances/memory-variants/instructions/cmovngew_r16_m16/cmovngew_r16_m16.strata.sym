code: cmovngew (%rax), %bx

  maybe read:      { %rax %sf %of }
  must read:       { %rax %sf %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovngew (%rax), %bx: Hindex0(10)
cmovngew (%rax), %bx: Hindex0(10)
cmovngew %cx, %bx: Hindex3(9)
Formula:

%rbx   : %rbx[63:16] ∘ (!(%sf ↔ %of) ? TMP_BV_16_0 : %rbx[15:0])

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rax.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
