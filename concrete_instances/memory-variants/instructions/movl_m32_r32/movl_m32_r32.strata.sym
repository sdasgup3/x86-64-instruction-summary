code: movl %ebx, (%rax)

  maybe read:      { %rax %ebx }
  must read:       { %rax %ebx }
  maybe write:     { }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movl %ebx, (%rax): Hindex0(10)
movl %ebx, (%rax): Hindex0(10)
movl %ecx, %ebx: Hindex5(9)
Formula:

Information about memory writes:
  Address %rax was updated to
    %rbx[31:0] (4 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv ∨ !sigfpe ∧ !sigbus ∧ false
