code: setg (%rbx)

  maybe read:      { %rbx %zf %sf %of }
  must read:       { %rbx %zf %sf %of }
  maybe write:     { }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

setg (%rbx): Hindex3(9)
setg (%rbx): Hindex3(9)
Formula:

Information about memory writes:
  Address %rbx was updated to
    (ite (and (not %zf) (= %sf %of)) #x01 #x00) (1 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (or sigsegv (and (not sigfpe) (not sigbus) false))
