code: cmovnow (%rcx), %bx

  maybe read:      { %rcx %of }
  must read:       { %rcx %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnow (%rcx), %bx: Hindex3(9)
cmovnow (%rcx), %bx: Hindex3(9)
Formula:

%rbx   : (concat ((_ extract 63 16) %rbx)
        (ite (not %of) TMP_BV_16_0 ((_ extract 15 0) %rbx)))

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rcx.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (or sigsegv (and (not sigfpe) (not sigbus) false))
