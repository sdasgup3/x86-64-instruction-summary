code: setnp (%rbx)

  maybe read:      { %rbx %pf }
  must read:       { %rbx %pf }
  maybe write:     { }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

Information about memory writes:
  Address %rbx was updated to
    (#ifMInt (notBool %pf) #then mi(8, 1) #else mi(8, 0) #fi) (1 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (sigsegv orBool (((notBool sigfpe) andBool (notBool sigbus)) andBool FALSE))
