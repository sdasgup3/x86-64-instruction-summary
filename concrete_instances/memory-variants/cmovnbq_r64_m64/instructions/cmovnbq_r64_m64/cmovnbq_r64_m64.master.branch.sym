code: cmovnbq (%rcx), %rbx

  maybe read:      { %rcx %cf }
  must read:       { %rcx %cf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnbq (%rcx), %rbx: Hindex3(9)
cmovnbq (%rcx), %rbx: Hindex3(9)
Formula:

%rbx   : (ite (not %cf) TMP_BV_64_0 %rbx)

Information about memory reads:
  Value TMP_BV_64_0 (8 bytes)
    was read at address %rcx.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (or sigsegv (and (not sigfpe) (not sigbus) false))
