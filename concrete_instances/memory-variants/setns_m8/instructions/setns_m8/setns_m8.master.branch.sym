code: setns (%rbx)

  maybe read:      { %rbx %sf }
  must read:       { %rbx %sf }
  maybe write:     { }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

setns (%rbx): Hindex3(9)
setns (%rbx): Hindex3(9)
Formula:

Information about memory writes:
  Address %rbx was updated to
    (ite (not %sf) #x01 #x00) (1 bytes).

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : (or sigsegv (and (not sigfpe) (not sigbus) false))
