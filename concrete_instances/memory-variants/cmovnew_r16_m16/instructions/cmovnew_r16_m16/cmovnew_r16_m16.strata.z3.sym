code: cmovnew (%rcx), %bx

  maybe read:      { %rcx %zf }
  must read:       { %rcx %zf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (concat ((_ extract 63 16) %rbx) (ite %zf ((_ extract 15 0) %rbx) TMP_BV_16_0))

Information about memory reads:
  Value TMP_BV_16_0 (2 bytes)
    was read at address %rcx.

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
