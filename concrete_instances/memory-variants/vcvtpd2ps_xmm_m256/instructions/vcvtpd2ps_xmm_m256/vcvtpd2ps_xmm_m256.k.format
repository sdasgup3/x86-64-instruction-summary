code: vcvtpd2ps (%rbx), %xmm1

  maybe read:      { %rbx %mxcsr::rc[0] }
  must read:       { %rbx %mxcsr::rc[0] }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(128, 0), concatenateMInt( cvt_double_to_single(extractMInt( %ymm1, 0, 64)), concatenateMInt( cvt_double_to_single(extractMInt( %ymm1, 64, 128)), concatenateMInt( cvt_double_to_single(extractMInt( %ymm1, 128, 192)), cvt_double_to_single(extractMInt( %ymm1, 192, 256))))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
