code: cmovneq %rcx, %rbx

  maybe read:      { %rcx %zf }
  must read:       { %rcx %zf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (ite (= (ite %zf #x02 #x00) #x00) %rcx %rbx)


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

