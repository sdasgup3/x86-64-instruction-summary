code: cmovneq %rcx, %rbx

  maybe read:      { %rcx %zf }
  must read:       { %rcx %zf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovneq %rcx, %rbx: Hindex3(9)
cmovneq %rcx, %rbx: Hindex3(9)
Formula:

%rbx   : (ite (not %zf) %rcx %rbx)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
