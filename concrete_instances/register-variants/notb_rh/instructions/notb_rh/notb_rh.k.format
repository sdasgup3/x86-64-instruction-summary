code: notb %ah

  maybe read:      { %ah }
  must read:       { %ah }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : concatenateMInt( concatenateMInt( extractMInt( %rax, 0, 48), xorMInt( mi(8, 255), extractMInt( %rax, 48, 56))), extractMInt( %rax, 56, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
