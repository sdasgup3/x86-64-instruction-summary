code: movzwq %cx, %rbx

  maybe read:      { %cx }
  must read:       { %cx }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( mi(48, 0), extractMInt( %rcx, 48, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
