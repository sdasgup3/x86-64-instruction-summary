code: cmovaew %cx, %bx

  maybe read:      { %cx %cf }
  must read:       { %cx %cf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), (#ifMInt (notBool %cf) #then extractMInt( %rcx, 48, 64) #else extractMInt( %rbx, 48, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
