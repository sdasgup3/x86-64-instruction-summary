code: movzbw %cl, %bx

  maybe read:      { %cl }
  must read:       { %cl }
  maybe write:     { %bx }
  must write:      { %bx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), concatenateMInt( mi(8, 0), extractMInt( %rcx, 56, 64)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
