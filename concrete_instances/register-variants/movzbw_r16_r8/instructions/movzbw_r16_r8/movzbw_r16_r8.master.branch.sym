code: movzbw %cl, %bx

  maybe read:      { %cl }
  must read:       { %cl }
  maybe write:     { %bx }
  must write:      { %bx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movzbw %cl, %bx: Hindex5(9)
movzbw %cl, %bx: Hindex5(9)
Formula:

%rbx   : (concat ((_ extract 63 16) %rbx) (concat #x00 ((_ extract 7 0) %rcx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
