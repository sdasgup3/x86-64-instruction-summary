code: cqto 

  maybe read:      { %rax }
  must read:       { %rax }
  maybe write:     { %rdx }
  must write:      { %rdx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rdx   : extractMInt( ashrMInt( concatenateMInt( %rax, mi(1, 0)), uvalueMInt(concatenateMInt( mi(57, 0), mi(8, 63)))), 0, 64)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
