code: setz %bl

  maybe read:      { %zf }
  must read:       { %zf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (#ifMInt %zf #then concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 1)) #else concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 0)) #fi)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
