code: cmovgq %rcx, %rbx

  maybe read:      { %rcx %zf %sf %of }
  must read:       { %rcx %zf %sf %of }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovgq %rcx, %rbx: Hindex3(9)
cmovgq %rcx, %rbx: Hindex3(9)
Formula:

%rbx   : (ite (and (not %zf) (= %sf %of)) %rcx %rbx)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
