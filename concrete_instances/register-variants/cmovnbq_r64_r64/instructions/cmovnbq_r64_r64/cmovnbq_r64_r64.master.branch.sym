code: cmovnbq %rcx, %rbx

  maybe read:      { %rcx %cf }
  must read:       { %rcx %cf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnbq %rcx, %rbx: Hindex3(9)
cmovnbq %rcx, %rbx: Hindex3(9)
Formula:

%rbx   : (ite (not %cf) %rcx %rbx)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
