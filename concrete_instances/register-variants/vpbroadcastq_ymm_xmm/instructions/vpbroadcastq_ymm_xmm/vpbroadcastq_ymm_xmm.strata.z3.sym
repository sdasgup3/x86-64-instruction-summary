code: vpbroadcastq %xmm2, %ymm1

  maybe read:      { %xmm2 }
  must read:       { %xmm2 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx2 }

Formula:

%ymm1  : (concat (concat ((_ extract 63 0) %ymm2) ((_ extract 63 0) %ymm2))
        (concat ((_ extract 63 0) %ymm2) ((_ extract 63 0) %ymm2)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
