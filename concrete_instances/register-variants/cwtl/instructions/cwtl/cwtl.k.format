code: cwtl 

  maybe read:      { %ax }
  must read:       { %ax }
  maybe write:     { %rax }
  must write:      { %rax }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : concatenateMInt( mi(32, 0), extractMInt( mi(64, svalueMInt(extractMInt( %rax, 48, 64))), 32, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
