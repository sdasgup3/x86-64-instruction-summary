code: cwtd 

  maybe read:      { %ax }
  must read:       { %ax }
  maybe write:     { %dx }
  must write:      { %dx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rdx   : concatenateMInt( extractMInt( %rdx, 0, 48), extractMInt( mi(32, svalueMInt(extractMInt( %rax, 48, 64))), 0, 16))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
