code: setnp %bl

  maybe read:      { %pf }
  must read:       { %pf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), (#ifMInt eqMInt( (#ifMInt %pf #then mi(16, 1) #else mi(16, 0)#fi), mi(16, 0)) #then mi(8, 1) #else mi(8, 0)#fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
