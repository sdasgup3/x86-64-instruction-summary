code: setnp %bl

  maybe read:      { %pf }
  must read:       { %pf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), (#ifMInt (notBool %pf) #then mi(8, 1) #else mi(8, 0) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
