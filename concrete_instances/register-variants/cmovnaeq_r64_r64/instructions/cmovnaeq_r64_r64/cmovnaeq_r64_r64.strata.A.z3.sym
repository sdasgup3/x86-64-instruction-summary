code: cmovnaeq %rcx, %rbx

  maybe read:      { %rcx %cf }
  must read:       { %rcx %cf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (let ((a!1 (= (ite (= (ite %cf #x00000100 #x00000000) #x00000000) #x02 #x00)
              #x00)))
  (ite a!1 %rcx %rbx))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

