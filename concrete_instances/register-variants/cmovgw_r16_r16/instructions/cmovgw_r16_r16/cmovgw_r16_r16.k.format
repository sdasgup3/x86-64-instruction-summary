code: cmovgw %cx, %bx

  maybe read:      { %cx %zf %sf %of }
  must read:       { %cx %zf %sf %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), (#ifMInt %zf #then extractMInt( %rbx, 48, 64) #else (#ifMInt eqMInt( xorMInt( (#ifMInt %of #then mi(1, 1) #else mi(1, 0) #fi), (#ifMInt %sf #then mi(1, 1) #else mi(1, 0) #fi)), mi(1, 1)) #then extractMInt( %rbx, 48, 64) #else extractMInt( %rcx, 48, 64) #fi) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
