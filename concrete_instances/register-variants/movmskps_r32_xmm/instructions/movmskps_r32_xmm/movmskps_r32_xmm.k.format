code: movmskps %xmm1, %ebx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse }

Formula:

%rbx   : concatenateMInt( mi(60, 0), concatenateMInt( extractMInt( %ymm1, 128, 129), concatenateMInt( extractMInt( %ymm1, 160, 161), concatenateMInt( extractMInt( %ymm1, 192, 193), extractMInt( %ymm1, 224, 225)))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
