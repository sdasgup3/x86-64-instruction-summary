code: notw %bx

  maybe read:      { %bx }
  must read:       { %bx }
  maybe write:     { %bx }
  must write:      { %bx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), xorMInt( extractMInt( %rbx, 48, 64), mi(16, 65535)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
