code: movq %xmm1, %rbx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Formula:

%rbx   : extractMInt( %ymm1, 192, 256)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
