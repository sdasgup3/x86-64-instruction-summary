code: setpo %bl

  maybe read:      { %pf }
  must read:       { %pf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

setpo %bl: Hindex3(9)
setpo %bl: Hindex3(9)
Formula:

%rbx   : (concat ((_ extract 63 8) %rbx) (ite (not %pf) #x01 #x00))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
