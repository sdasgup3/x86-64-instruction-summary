code: cmovnsl %ecx, %ebx

  maybe read:      { %ecx %sf }
  must read:       { %ecx %sf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( mi(32, 0), (#ifMInt eqMInt( (#ifMInt %sf #then mi(32, 0) #else mi(32, 4294967295) #fi), mi(32, 0)) #then extractMInt( %rbx, 32, 64) #else extractMInt( %rcx, 32, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
