code: cmovnsl %ecx, %ebx

  maybe read:      { %ecx %sf }
  must read:       { %ecx %sf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( mi(32, 0), (#ifMInt (notBool %sf) #then extractMInt( %rcx, 32, 64) #else extractMInt( %rbx, 32, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
