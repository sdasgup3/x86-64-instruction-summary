code: cmovnaw %cx, %bx

  maybe read:      { %cx %cf %zf }
  must read:       { %cx %cf %zf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), (#ifMInt (%cf orBool %zf) #then extractMInt( %rcx, 48, 64) #else extractMInt( %rbx, 48, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
