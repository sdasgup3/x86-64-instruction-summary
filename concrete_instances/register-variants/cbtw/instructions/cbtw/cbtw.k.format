code: cbtw 

  maybe read:      { %al }
  must read:       { %al }
  maybe write:     { %ax }
  must write:      { %ax }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : concatenateMInt( extractMInt( %rax, 0, 48), mi(16, svalueMInt(extractMInt( %rax, 56, 64))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
