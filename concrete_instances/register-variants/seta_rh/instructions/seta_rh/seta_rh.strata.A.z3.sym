code: seta %ah

  maybe read:      { %cf %zf }
  must read:       { %cf %zf }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : (let ((a!1 (concat ((_ extract 63 16) %rax)
                   (ite (= (ite %zf #x00 #xff) #x00) #x00 (ite %cf #x00 #x01)))))
  (concat a!1 ((_ extract 7 0) %rax)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

