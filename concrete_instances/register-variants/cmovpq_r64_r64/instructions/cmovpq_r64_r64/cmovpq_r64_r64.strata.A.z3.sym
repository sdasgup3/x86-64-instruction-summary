code: cmovpq %rcx, %rbx

  maybe read:      { %rcx %pf }
  must read:       { %rcx %pf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (let ((a!1 (= (ite (= (ite %pf #x00 #x02) #x00) #x00000001 #x00000000)
              #x00000000)))
  (ite (= (ite a!1 #x02 #x00) #x00) %rcx %rbx))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

