code: setns %ah

  maybe read:      { %sf }
  must read:       { %sf }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : (concat (concat ((_ extract 63 16) %rax) (ite (not %sf) #x01 #x00))
        ((_ extract 7 0) %rax))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

