code: cmovpol %ecx, %ebx

  maybe read:      { %ecx %pf }
  must read:       { %ecx %pf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovpol %ecx, %ebx: Hindex3(9)
cmovpol %ecx, %ebx: Hindex3(9)
Formula:

%rbx   : (concat #x00000000
        (ite (not %pf) ((_ extract 31 0) %rcx) ((_ extract 31 0) %rbx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
