code: cmovsw %cx, %bx

  maybe read:      { %cx %sf }
  must read:       { %cx %sf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (concat ((_ extract 63 16) %rbx)
        (ite (= (ite %sf #x00000000 #xffffffff) #x00000000)
             ((_ extract 15 0) %rcx)
             ((_ extract 15 0) %rbx)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

