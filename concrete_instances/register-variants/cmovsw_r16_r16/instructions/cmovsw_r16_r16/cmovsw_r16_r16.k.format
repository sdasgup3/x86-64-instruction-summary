code: cmovsw %cx, %bx

  maybe read:      { %cx %sf }
  must read:       { %cx %sf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), (#ifMInt %sf #then extractMInt( %rcx, 48, 64) #else extractMInt( %rbx, 48, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
