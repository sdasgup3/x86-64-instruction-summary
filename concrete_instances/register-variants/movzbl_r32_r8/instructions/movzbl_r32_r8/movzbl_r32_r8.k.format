code: movzbl %cl, %ebx

  maybe read:      { %cl }
  must read:       { %cl }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( mi(56, 0), extractMInt( addMInt( concatenateMInt( mi(1, 0), extractMInt( %rcx, 56, 64)), concatenateMInt( mi(8, 0), getParity(mi(8, 16)))), 1, 9))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
