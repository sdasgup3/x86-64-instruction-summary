code: movzbl %cl, %ebx

  maybe read:      { %cl }
  must read:       { %cl }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

movzbl %cl, %ebx: Hindex5(9)
movzbl %cl, %ebx: Hindex5(9)
Formula:

%rbx   : (concat #x00000000 (concat #x000000 ((_ extract 7 0) %rcx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
