code: cvtsi2sdq %rbx, %xmm1

  maybe read:      { %rbx %xmm1 %mxcsr::rc[0] }
  must read:       { %rbx %xmm1 %mxcsr::rc[0] }
  maybe write:     { %xmm1 }
  must write:      { %xmm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Formula:

%ymm1  : concatenateMInt( extractMInt( %ymm1, 0, 192), cvt_int64_to_double(%rbx))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
