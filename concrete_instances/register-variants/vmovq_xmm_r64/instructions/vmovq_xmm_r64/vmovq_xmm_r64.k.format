code: vmovq %rbx, %xmm1

  maybe read:      { %rbx }
  must read:       { %rbx }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(192, 0), xorMInt( %rbx, mi(64, 0)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
