code: vmovq %rbx, %xmm1

  maybe read:      { %rbx }
  must read:       { %rbx }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(192, 0), xorMInt( mi(64, 0), %rbx))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
