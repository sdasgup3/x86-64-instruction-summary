code: cmovngq %rcx, %rbx

  maybe read:      { %rcx %zf %sf %of }
  must read:       { %rcx %zf %sf %of }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovngq %rcx, %rbx: Hindex3(9)
cmovngq %rcx, %rbx: Hindex3(9)
Formula:

%rbx   : (ite (or %zf (not (= %sf %of))) %rcx %rbx)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
