code: seta %bl

  maybe read:      { %cf %zf }
  must read:       { %cf %zf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), (#ifMInt eqMInt( (#ifMInt %zf #then mi(8, 0) #else mi(8, 255) #fi), mi(8, 0)) #then mi(8, 0) #else (#ifMInt %cf #then mi(8, 0) #else mi(8, 1) #fi) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
