code: seta %bl

  maybe read:      { %cf %zf }
  must read:       { %cf %zf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (concat ((_ extract 63 8) %rbx)
        (ite (= (ite %zf #x00 #xff) #x00) #x00 (ite %cf #x00 #x01)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

