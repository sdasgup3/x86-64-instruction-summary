code: sarxl %edx, %ecx, %ebx

  maybe read:      { %ecx %edx }
  must read:       { %ecx %edx }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { bmi2 }

Formula:

%rbx   : concatenateMInt( mi(32, 0), aShiftRightMInt( extractMInt( %rcx, 32, 64), uvalueMInt(andMInt( extractMInt( %rdx, 32, 64), mi(32, 31)))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
