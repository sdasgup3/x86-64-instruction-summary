code: cvtss2siq %xmm1, %rbx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse }

Formula:

%rbx   : cvt_single_to_int64(extractMInt( %ymm1, 224, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
