code: setae %bl

  maybe read:      { %cf }
  must read:       { %cf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (#ifMInt eqMInt( (#ifMInt %cf #then mi(8, 1) #else mi(8, 0) #fi), mi(8, 0)) #then concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 1)) #else concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 0)) #fi)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
