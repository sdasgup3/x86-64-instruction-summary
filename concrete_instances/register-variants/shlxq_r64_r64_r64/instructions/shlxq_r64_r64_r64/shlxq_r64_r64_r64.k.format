code: shlxq %rdx, %rcx, %rbx

  maybe read:      { %rcx %rdx }
  must read:       { %rcx %rdx }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { bmi2 }

Formula:

%rbx   : shlMInt( %rcx, uvalueMInt(andMInt( %rdx, mi(64, 63))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
