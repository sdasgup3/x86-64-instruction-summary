code: setna %ah

  maybe read:      { %cf %zf }
  must read:       { %cf %zf }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : concatenateMInt( concatenateMInt( extractMInt( %rax, 0, 48), (#ifMInt (%cf orBool %zf) #then mi(8, 1) #else mi(8, 0) #fi)), extractMInt( %rax, 56, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
