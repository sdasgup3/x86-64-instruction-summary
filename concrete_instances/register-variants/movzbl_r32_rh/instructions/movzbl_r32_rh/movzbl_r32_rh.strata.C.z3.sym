code: movzbl %ah, %ebx

  maybe read:      { %ah }
  must read:       { %ah }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (concat #x00000000 (concat #x000000 ((_ extract 15 8) %rax)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
