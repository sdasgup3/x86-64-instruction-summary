code: movzbl %ah, %ebx

  maybe read:      { %ah }
  must read:       { %ah }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( mi(56, 0), extractMInt( %rax, 48, 56))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
