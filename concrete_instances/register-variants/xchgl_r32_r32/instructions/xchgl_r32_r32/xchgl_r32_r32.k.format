code: xchgl %ecx, %ebx

  maybe read:      { %ecx %ebx }
  must read:       { %ecx %ebx }
  maybe write:     { %rcx %rbx }
  must write:      { %rcx %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rcx   : concatenateMInt( mi(32, 0), extractMInt( %rbx, 32, 64))
%rbx   : concatenateMInt( mi(32, 0), extractMInt( %rcx, 32, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
