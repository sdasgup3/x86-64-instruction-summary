code: vcvttsd2siq %xmm1, %rbx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%rbx   : cvt_double_to_int64_truncate(extractMInt( %ymm1, 192, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
