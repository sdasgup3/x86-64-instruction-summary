code: cvtsd2sil %xmm1, %ebx

  maybe read:      { %xmm1 %mxcsr::rc[0] }
  must read:       { %xmm1 %mxcsr::rc[0] }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Formula:

%rbx   : concatenateMInt( mi(32, 0), cvt_double_to_int32(extractMInt( %ymm1, 192, 256)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
