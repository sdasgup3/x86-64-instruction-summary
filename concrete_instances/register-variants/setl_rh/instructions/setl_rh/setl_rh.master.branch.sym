code: setl %ah

  maybe read:      { %sf %of }
  must read:       { %sf %of }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

setl %ah: Hindex3(9)
setl %ah: Hindex3(9)
Formula:

%rax   : (let ((a!1 (concat ((_ extract 63 16) %rax) (ite (not (= %sf %of)) #x01 #x00))))
  (concat a!1 ((_ extract 7 0) %rax)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
