code: vcvttpd2dq %xmm2, %xmm1

  maybe read:      { %xmm2 }
  must read:       { %xmm2 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(192, 0), concatenateMInt( cvt_double_to_int32_truncate(extractMInt( %ymm2, 128, 192)), cvt_double_to_int32_truncate(extractMInt( %ymm2, 192, 256))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
