code: notb %bl

  maybe read:      { %bl }
  must read:       { %bl }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), xorMInt( extractMInt( %rbx, 56, 64), mi(8, 255)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
