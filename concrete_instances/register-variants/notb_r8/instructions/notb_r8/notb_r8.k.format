code: notb %bl

  maybe read:      { %bl }
  must read:       { %bl }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), negMInt( extractMInt( %rbx, 56, 64)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
