code: setng %ah

  maybe read:      { %zf %sf %of }
  must read:       { %zf %sf %of }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : (let ((a!1 (ite (or %zf (not (= %sf %of))) #x01 #x00)))
  (concat (concat ((_ extract 63 16) %rax) a!1) ((_ extract 7 0) %rax)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

