code: setng %ah

  maybe read:      { %zf %sf %of }
  must read:       { %zf %sf %of }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : (let ((a!1 (= (concat (bvxor (ite %of #x01 #x00) (ite %sf #x01 #x00))
                      (ite %zf #x01 #x00))
              #x0000)))
  (concat (concat ((_ extract 63 16) %rax) (ite a!1 #x00 #x01))
          ((_ extract 7 0) %rax)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

