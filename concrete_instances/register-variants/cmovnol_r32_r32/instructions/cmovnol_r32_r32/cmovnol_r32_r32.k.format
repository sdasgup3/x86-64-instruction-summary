code: cmovnol %ecx, %ebx

  maybe read:      { %ecx %of }
  must read:       { %ecx %of }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( mi(32, 0), (#ifMInt (notBool %of) #then extractMInt( %rcx, 32, 64) #else extractMInt( %rbx, 32, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
