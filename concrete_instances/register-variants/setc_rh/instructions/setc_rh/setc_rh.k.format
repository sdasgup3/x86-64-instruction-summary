code: setc %ah

  maybe read:      { %cf }
  must read:       { %cf }
  maybe write:     { %ah }
  must write:      { %ah }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : (#ifMInt %cf #then concatenateMInt( concatenateMInt( extractMInt( %rax, 0, 48), mi(8, 1)), extractMInt( %rax, 56, 64)) #else concatenateMInt( concatenateMInt( extractMInt( %rax, 0, 48), mi(8, 0)), extractMInt( %rax, 56, 64)) #fi)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
