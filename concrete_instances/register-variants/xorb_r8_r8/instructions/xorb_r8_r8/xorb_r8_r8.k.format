code: xorb %cl, %bl

  maybe read:      { %cl %bl }
  must read:       { %cl %bl }
  maybe write:     { %bl %cf %pf %zf %sf %of }
  must write:      { %bl %cf %pf %zf %sf %of }
  maybe undef:     { %af }
  must undef:      { %af }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), xorMInt( extractMInt( %rcx, 56, 64), extractMInt( %rbx, 56, 64)))

%cf    : mi(1, 0)
%pf    : getParity(xorMInt( extractMInt( %rcx, 56, 64), extractMInt( %rbx, 56, 64)))
%af    : (#ifMInt TMP_BOOL_3 #then mi(1, 1) #else mi(1, 0) #fi)
%zf    : (#ifMInt eqMInt( xorMInt( extractMInt( %rcx, 56, 64), extractMInt( %rbx, 56, 64)), mi(8, 0)) #then mi(1, 1) #else mi(1, 0) #fi)
%sf    : xorMInt( extractMInt( %rcx, 56, 57), extractMInt( %rbx, 56, 57))
%of    : mi(1, 0)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
