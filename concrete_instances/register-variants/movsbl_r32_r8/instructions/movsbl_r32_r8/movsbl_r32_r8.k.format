code: movsbl %cl, %ebx

  maybe read:      { %cl }
  must read:       { %cl }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( mi(32, 0), extractMInt( mi(64, svalueMInt(extractMInt( %rcx, 56, 64)), 32, 64))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
