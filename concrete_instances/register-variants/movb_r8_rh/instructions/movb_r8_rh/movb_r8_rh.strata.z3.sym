code: movb %ah, %bl

  maybe read:      { %ah }
  must read:       { %ah }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (concat ((_ extract 63 8) %rbx) ((_ extract 15 8) %rax))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
