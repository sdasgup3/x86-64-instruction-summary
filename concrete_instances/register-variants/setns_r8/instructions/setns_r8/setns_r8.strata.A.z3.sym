code: setns %bl

  maybe read:      { %sf }
  must read:       { %sf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (concat ((_ extract 63 8) %rbx)
        (ite (= (ite %sf #x0000000000000001 #x0000000000000000)
                #x0000000000000000)
             #x01
             #x00))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
