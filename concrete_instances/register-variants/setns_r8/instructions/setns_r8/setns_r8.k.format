code: setns %bl

  maybe read:      { %sf }
  must read:       { %sf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), (#ifMInt eqMInt( (#ifMInt %sf #then mi(64, 1) #else mi(64, 0) #fi), mi(64, 0)) #then mi(8, 1) #else mi(8, 0) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
