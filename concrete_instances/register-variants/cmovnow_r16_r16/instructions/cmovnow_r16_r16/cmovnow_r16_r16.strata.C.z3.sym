code: cmovnow %cx, %bx

  maybe read:      { %cx %of }
  must read:       { %cx %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnow %cx, %bx: Hindex3(9)
cmovnow %cx, %bx: Hindex3(9)
Formula:

%rbx   : (concat ((_ extract 63 16) %rbx)
        (ite (not %of) ((_ extract 15 0) %rcx) ((_ extract 15 0) %rbx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
