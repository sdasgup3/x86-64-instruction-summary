code: cmovngl %ecx, %ebx

  maybe read:      { %ecx %zf %sf %of }
  must read:       { %ecx %zf %sf %of }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : concatenateMInt( mi(32, 0), (#ifMInt (%zf orBool (notBool (%sf ==Bool %of))) #then extractMInt( %rcx, 32, 64) #else extractMInt( %rbx, 32, 64) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
