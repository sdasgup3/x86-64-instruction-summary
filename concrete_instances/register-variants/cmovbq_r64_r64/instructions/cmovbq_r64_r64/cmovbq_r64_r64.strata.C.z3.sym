code: cmovbq %rcx, %rbx

  maybe read:      { %rcx %cf }
  must read:       { %rcx %cf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovbq %rcx, %rbx: Hindex3(9)
cmovbq %rcx, %rbx: Hindex3(9)
Formula:

%rbx   : (ite %cf %rcx %rbx)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
