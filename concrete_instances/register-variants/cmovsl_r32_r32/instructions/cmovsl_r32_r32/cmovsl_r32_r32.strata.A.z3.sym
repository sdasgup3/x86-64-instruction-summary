code: cmovsl %ecx, %ebx

  maybe read:      { %ecx %sf }
  must read:       { %ecx %sf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (concat #x00000000
        (ite (= (ite %sf #b0 #b1) #b1)
             ((_ extract 31 0) %rbx)
             ((_ extract 31 0) %rcx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
