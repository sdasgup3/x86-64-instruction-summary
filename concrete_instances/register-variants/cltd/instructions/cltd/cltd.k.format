code: cltd 

  maybe read:      { %eax }
  must read:       { %eax }
  maybe write:     { %rdx }
  must write:      { %rdx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rdx   : concatenateMInt( mi(32, 0), xorMInt( mi(32, 0), extractMInt( mi(64, svalueMInt(extractMInt( %rax, 32, 64))), 0, 32)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
