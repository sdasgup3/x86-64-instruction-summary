code: cltd 

  maybe read:      { %eax }
  must read:       { %eax }
  maybe write:     { %rdx }
  must write:      { %rdx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rdx   : (let ((a!1 (bvxor #x00000000
                  ((_ extract 63 32)
                    ((_ sign_extend 32) ((_ extract 31 0) %rax))))))
  (concat #x00000000 a!1))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
