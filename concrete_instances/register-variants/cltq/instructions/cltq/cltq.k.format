code: cltq 

  maybe read:      { %eax }
  must read:       { %eax }
  maybe write:     { %rax }
  must write:      { %rax }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rax   : signExtend( extractMInt( %rax, 32, 64), 64)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
