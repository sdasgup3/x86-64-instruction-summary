code: setnl %bl

  maybe read:      { %sf %of }
  must read:       { %sf %of }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (concat ((_ extract 63 8) %rbx)
        (bvxor #x01 (ite %of #x01 #x00) (ite %sf #x01 #x00)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
