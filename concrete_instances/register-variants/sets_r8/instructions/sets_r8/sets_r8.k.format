code: sets %bl

  maybe read:      { %sf }
  must read:       { %sf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 56), (#ifMInt %sf #then mi(8, 1) #else mi(8, 0)#fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
