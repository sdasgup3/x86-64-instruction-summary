code: sets %bl

  maybe read:      { %sf }
  must read:       { %sf }
  maybe write:     { %bl }
  must write:      { %bl }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : (#ifMInt %sf #then concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 1)) #else concatenateMInt( extractMInt( %rbx, 0, 56), mi(8, 0)) #fi)

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
