code: rorl $0x1, %ebx

  maybe read:      { %ebx }
  must read:       { %ebx }
  maybe write:     { %rbx %cf %of }
  must write:      { %rbx %cf %of }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

rorl $0x1, %ebx: Hindex7(9)
rorl $0x1, %ebx: Hindex7(9)
Formula:

%rbx   : (concat #x00000000 (ext_rotate_right ((_ extract 31 0) %rbx) #x00000001))

%cf    : (let ((a!1 (= ((_ extract 31 31)
                (ext_rotate_right ((_ extract 31 0) %rbx) #x00000001))
              #b1)))
  (or (and true a!1) (and false %cf)))
%of    : (let ((a!1 (= ((_ extract 31 31)
                (ext_rotate_right ((_ extract 31 0) %rbx) #x00000001))
              #b1))
      (a!2 (= ((_ extract 30 30)
                (ext_rotate_right ((_ extract 31 0) %rbx) #x00000001))
              #b1)))
  (or (and true (xor a!1 a!2)) (and false TMP_BOOL_0)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
