code: pmovsxbq %xmm2, %xmm1

  maybe read:      { %xmm2 }
  must read:       { %xmm2 }
  maybe write:     { %xmm1 }
  must write:      { %xmm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse4_1 }

Formula:

%ymm1  : concatenateMInt( extractMInt( %ymm1, 0, 128), concatenateMInt( mi(64, svalueMInt(extractMInt( %ymm2, 240, 248))), mi(64, svalueMInt(extractMInt( %ymm2, 248, 256)))))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
