code: movzbw %ah, %bx

  maybe read:      { %ah }
  must read:       { %ah }
  maybe write:     { %bx }
  must write:      { %bx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { }

Formula:

%rbx   : concatenateMInt( extractMInt( %rbx, 0, 48), concatenateMInt( mi(8, 0), extractMInt( %rax, 48, 56)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
