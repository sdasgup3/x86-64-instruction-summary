code: cmovnbl %ecx, %ebx

  maybe read:      { %ecx %cf }
  must read:       { %ecx %cf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

cmovnbl %ecx, %ebx: Hindex3(9)
cmovnbl %ecx, %ebx: Hindex3(9)
Formula:

%rbx   : (concat #x00000000
        (ite (not %cf) ((_ extract 31 0) %rcx) ((_ extract 31 0) %rbx)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
