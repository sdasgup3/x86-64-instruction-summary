code: cmovngew %cx, %bx

  maybe read:      { %cx %sf %of }
  must read:       { %cx %sf %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Formula:

%rbx   : (concat ((_ extract 63 16) %rbx)
        (ite (not (= %sf %of)) ((_ extract 15 0) %rcx) ((_ extract 15 0) %rbx)))


sigfpe  : sigfpe

sigbus  : sigbus

sigsegv : sigsegv

