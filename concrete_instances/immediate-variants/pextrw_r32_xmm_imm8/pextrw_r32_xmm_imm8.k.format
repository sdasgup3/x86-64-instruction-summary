code: pextrw $0x0, %xmm1, %ebx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Formula:

%rbx   : concatenateMInt( mi(48, 0), extractMInt( %ymm1, 240, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
