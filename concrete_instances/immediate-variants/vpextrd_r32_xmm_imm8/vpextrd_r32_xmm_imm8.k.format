code: vpextrd $0x0, %xmm1, %ebx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%rbx   : concatenateMInt( mi(32, 0), extractMInt( %ymm1, 224, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
