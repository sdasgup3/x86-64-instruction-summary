code: vextractf128 $0x0, %ymm1, %xmm1

  maybe read:      { %ymm1 }
  must read:       { %ymm1 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(128, 0), extractMInt( %ymm1, 128, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
