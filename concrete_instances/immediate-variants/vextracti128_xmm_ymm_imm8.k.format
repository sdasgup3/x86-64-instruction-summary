code: vextracti128 $0x0, %ymm1, %xmm1

  maybe read:      { %ymm1 }
  must read:       { %ymm1 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx2 }

Formula:

%ymm1  : concatenateMInt( mi(128, 0), (#ifMInt eqMInt( extractMInt( Imm8, 7, 8), mi(1, 0)) #then extractMInt( %ymm1, 128, 256) #else extractMInt( %ymm1, 0, 128) #fi))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
