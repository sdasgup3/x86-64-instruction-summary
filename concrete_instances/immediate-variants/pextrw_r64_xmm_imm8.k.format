code: pextrw $0x0, %xmm1, %rbx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Formula:

%rbx   : concatenateMInt( mi(48, 0), extractMInt( lshrMInt( extractMInt( %ymm1, 128, 256), uvalueMInt(mi(128, 0))), 112, 128))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
