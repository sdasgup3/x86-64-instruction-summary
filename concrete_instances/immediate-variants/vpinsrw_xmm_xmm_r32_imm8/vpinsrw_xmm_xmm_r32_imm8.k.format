code: vpinsrw $0x0, %ebx, %xmm2, %xmm1

  maybe read:      { %ebx %xmm2 }
  must read:       { %ebx %xmm2 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(128, 0), concatenateMInt( extractMInt( %ymm2, 128, 240), extractMInt( %rbx, 48, 64)))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
