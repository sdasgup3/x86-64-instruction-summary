code: vinsertps $0x0, %xmm3, %xmm2, %xmm1

  maybe read:      { %xmm1 %xmm2 %xmm3 }
  must read:       { %xmm1 %xmm2 %xmm3 }
  maybe write:     { %ymm1 %xmm2 }
  must write:      { %ymm1 %xmm2 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( mi(128, 0), concatenateMInt( extractMInt( %ymm2, 128, 224), extractMInt( %ymm3, 224, 256)))
%ymm2  : %ymm2

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
