code: vinsertf128 $0x0, %xmm3, %ymm2, %ymm1

  maybe read:      { %ymm2 %xmm3 }
  must read:       { %ymm2 %xmm3 }
  maybe write:     { %ymm1 }
  must write:      { %ymm1 }
  maybe undef:     { }
  must undef:      { }
  required flags:  { avx }

Formula:

%ymm1  : concatenateMInt( extractMInt( %ymm2, 0, 128), extractMInt( %ymm3, 128, 256))

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv
